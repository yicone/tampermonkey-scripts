{
  "name": "tampermonkey-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:probe": "tools/probe-adapters.sh"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
